export const CHANGE_SEARCH_FIELD = "CHANGE_SEARCH_FIELD"; // CHANGE_SEARCH_FIELD form the action fille

// Creating a constant for thunkmiddleware to request data from the API
// and it will have 3 steps.

export const REQUEST_ROBOTS_PENDING = "REQUEST_ROBOTS_PENDING"; //in constant strings should be the same
export const REQUEST_ROBOTS_SUCCESS = "REQUEST_ROBOTS_SUCCESS"; //in constant strings should be the same
export const REQUEST_ROBOTS_FAILED = "REQUEST_ROBOTS_FAILED"; //in constant strings should be the same

// Notice that this request has 3 actions and it is a standard in AJAX calls
// PENDING,SUCCESS, and FAILED

// Now let's import them in action file
